
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agencyFinal
    password: password
    username: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081
  error:
    path: /error
debug: true
